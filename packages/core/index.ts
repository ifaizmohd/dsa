import { twoSum } from '../techniques/two-pointer/questions/two-sum-ii';

console.log(
  '------------------------------------------------------------------------------'
);
const nums = [-1, 0];
const val = -1;
const res = twoSum(nums, val);
console.log(res);
console.log(
  '------------------------------------------------------------------------------'
);
