import { merge } from '../techniques/two-pointer/questions/merge-sorted-arrays';

console.log(
  '------------------------------------------------------------------------------'
);
const nums1 = [4, 5, 6, 0, 0, 0];
const val = -1;
const output = merge(nums1, 3, [1, 2, 3], 3);
console.log(nums1);
console.log(
  '------------------------------------------------------------------------------'
);
